version: '3.8'

services:
  freezer-manager:
    container_name: freezer-manager
    build: .
    ports:
      - "8080:80"
    volumes:
      - freezer-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  freezer-data:
    driver: local
